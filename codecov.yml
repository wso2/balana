coverage:
  status:
    project:
      default:
        target: 60%
        threshold: 0%
        base: auto
        flags:
          - unit
        paths:
          - "src"
        branches:
          - master
        if_ci_failed: error
        informational: true
        only_pulls: false
